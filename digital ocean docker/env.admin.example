# Admin Authentication Environment Variables
# ============================================
# Copy this file to env.production and update the values

# Admin Panel Authentication (Simple Auth)
# These are used ONLY for admin panel access
# They do NOT affect VPN client authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ViWorkS@2024!
ADMIN_SESSION_TTL_SECONDS=28800

# Backend API URL (for reference - not used for admin auth)
NEXT_PUBLIC_API_URL=https://api-viworks.neuratalent.com

# Database Configuration (for backend services only)
POSTGRES_PASSWORD=viworks_password_2024
DATABASE_URL=postgresql://admin:viworks_password_2024@postgres:5432/viworks

# Redis Configuration (for backend services only)
REDIS_URL=redis://redis:6379

# JWT Configuration (for VPN client auth - not admin auth)
JWT_SECRET=your-jwt-secret-here

# Other backend configuration
RUST_LOG=info
NODE_ENV=production
